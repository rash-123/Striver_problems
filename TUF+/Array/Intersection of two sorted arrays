https://takeuforward.org/plus/data-structures-and-algorithm/arrays/logic-building/intersection-of-two-sorted-arrays

//Optimal

class Solution {
public:
//TC - O(MXN)
//SC - O(N)
    vector<int> intersectionArray(vector<int>& nums1, vector<int>& nums2) {
        vector<int> visited(nums2.size(), 0);

        vector<int> ans;

        for(int i = 0; i < nums1.size(); i++){
            for(int j = 0; j < nums2.size(); j++){
                if(nums1[i] == nums2[j] && visited[j] == 0){
                    ans.push_back(nums1[i]);
                    visited[j] = 1;
                    break;
                } 
                else if(nums2[j] > nums1[i]){
                    break;
                }
            }
        }
        return ans;
    }
};

//Brute force
class Solution {
public:
//TC - O(MXN)
//SC - O(N)
    vector<int> intersectionArray(vector<int>& nums1, vector<int>& nums2) {
        vector<int> visited(nums2.size(), 0);

        vector<int> ans;

        for(int i = 0; i < nums1.size(); i++){
            for(int j = 0; j < nums2.size(); j++){
                if(nums1[i] == nums2[j] && visited[j] == 0){
                    ans.push_back(nums1[i]);
                    visited[j] = 1;
                    break;
                } 
                else if(nums2[j] > nums1[i]){
                    break;
                }
            }
        }
        return ans;
    }
};


